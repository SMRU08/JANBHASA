# Faster-Whisper INT8 (ASR)
Place the quantized CTranslate2/ONNX INT8 model files here:
- `model.bin` (or `whisper_small_int8.onnx`)
- `config.json`
- `vocabulary.json`
Target RAM footprint: ~145 MB.
